services:
  hub:
    build: .
    ports:
      - "8080:8080"
    environment:
      - LISTEN_ADDR=:8080
      # Use a YAML config file (optional):
      # - CONFIG_FILE=/app/config.yaml
      # Or pre-register environments via env var (legacy):
      # - ENVIRONMENTS=[{"id":"prod-default","name":"Production","cluster_id":"k8s-prod","namespace":"default"}]
    # volumes:
    #   - ./config.yaml:/app/config.yaml:ro

  # Example agent for a Docker Swarm cluster behind a firewall.
  # Deploy this in the target network:
  # agent:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile.agent
  #   environment:
  #     - HUB_SERVER=ws://hub:8080/ws/tunnel
  #     - CLUSTER_ID=swarm-dc1
  #     - CLUSTER_NAME=DC1 Swarm
  #     - CLUSTER_TYPE=docker-swarm
  #     - LOCAL_ENDPOINT=http://swarm-manager:2375
  #     - AUTH_TOKEN=
  #     - SKIP_TLS=false
